[phases.setup]
cmds = ["apt-get update", "apt-get install -y nodejs npm"]

[phases.install]
cmds = ["npm ci --production=false"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
